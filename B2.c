//Write a program to reverse a String Using the Stack Data structure.

#include <stdio.h>
#define MAX 20
char stack[MAX];
int top;
char ch;
void push(char ch);
char pop();
void display();

int main() {
  top = -1;
  char stack[MAX];
}
