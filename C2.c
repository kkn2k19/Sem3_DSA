//Write a program to convert Infix Expression to Postfix Expression.

NOT COMPLETE!!!!!
  
#include <stdio.h>
#define MAX 10
char stack[MAX];
int top;
void push (char ch);
char pop ();
void display();
void convertion(char exp[]);

int main () 
{
  top = -1;
  char exp[20];
  printf("Enter infix expression : ");
  gets(exp);
  convertion(infix);
  return 0;
}

void convertion (char exp[])
{
  
}
void push ()
